# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets. *.local already covers .env.local, but a plain .env is easy to create by
# habit and this repo is public — and every commit here is made with `git add -A`.
.env
.env.*
!.env.example

# Per-machine Claude Code permission state (not shared project config)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
